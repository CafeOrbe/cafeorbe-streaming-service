spring:
  datasource:
    url: jdbc:h2:mem:streaming;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  rabbitmq:
    listener:
      simple:
        auto-startup: false

cafeorbe:
  outbox:
    scheduler: false
  livekit:
    api-url: http://localhost:1
